# Uncomment to build separately
# project('hello', 'c')

gnome = import('gnome')

executable('hello',
  'hello.c',
  dependencies: [
    dependency('gtk4'),
    dependency('libadapta-1'),
  ],
  install: false,
)
